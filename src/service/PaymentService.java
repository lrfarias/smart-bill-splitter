package src.service;

import src.model.Bill;
import src.model.Person;

public class PaymentService {
    /*
     * generatePayments(Bill bill)
     * 
     * markAsPaid(Person person)
     * 
     * printPayments()
     */
}
